<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FitBot — Операционная система для фитнес-клуба в Telegram</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Golos+Text:wght@400;500;600;700;800;900&family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-dark: #0a0e1a;
            --bg-darker: #050810;
            --electric-blue: #00d9ff;
            --neon-pink: #ff0080;
            --energy-yellow: #ffed4e;
            --power-purple: #9d4edd;
            --text-white: #ffffff;
            --text-gray: #b8c5d6;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Golos Text', sans-serif;
            background: var(--bg-dark);
            color: var(--text-white);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* Animated Background Grid */
        .animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background:
                linear-gradient(90deg, rgba(0, 217, 255, 0.03) 1px, transparent 1px),
                linear-gradient(rgba(0, 217, 255, 0.03) 1px, transparent 1px);
            background-size: 60px 60px;
            animation: gridMove 20s linear infinite;
            z-index: -1;
        }

        @keyframes gridMove {
            0% { transform: translateY(0); }
            100% { transform: translateY(60px); }
        }

        /* Floating Energy Particles */
        .energy-particle {
            position: fixed;
            width: 4px;
            height: 4px;
            background: var(--electric-blue);
            border-radius: 50%;
            pointer-events: none;
            z-index: 0;
            animation: float 10s infinite ease-in-out;
            box-shadow: 0 0 20px var(--electric-blue);
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) translateX(0); opacity: 0; }
            25% { opacity: 1; }
            50% { transform: translateY(-100vh) translateX(50px); opacity: 0.8; }
            75% { opacity: 1; }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 800px;
            height: 800px;
            background: radial-gradient(circle, var(--power-purple) 0%, transparent 70%);
            transform: translate(-50%, -50%);
            opacity: 0.2;
            animation: pulse 4s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.2; }
            50% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.3; }
        }

        .hero-content {
            display: grid;
            grid-template-columns: 1.2fr 1fr;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .hero-text h1 {
            font-family: 'Manrope', sans-serif;
            font-size: clamp(3rem, 7vw, 6rem);
            font-weight: 900;
            line-height: 1.1;
            margin-bottom: 2rem;
            background: linear-gradient(135deg, var(--electric-blue), var(--neon-pink), var(--energy-yellow));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradientShift 8s ease infinite;
            background-size: 200% 200%;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .hero-text h2 {
            font-size: clamp(1.2rem, 2vw, 1.5rem);
            color: var(--text-gray);
            font-weight: 500;
            margin-bottom: 3rem;
            line-height: 1.8;
        }

        .highlight {
            color: var(--electric-blue);
            font-weight: 700;
        }

        .cta-btn {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            padding: 1.5rem 3rem;
            background: linear-gradient(135deg, var(--neon-pink), var(--power-purple));
            color: var(--text-white);
            font-size: 1.2rem;
            font-weight: 700;
            text-decoration: none;
            border-radius: 50px;
            box-shadow: 0 10px 40px rgba(255, 0, 128, 0.4);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .cta-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .cta-btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .cta-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 60px rgba(255, 0, 128, 0.6);
        }

        .cta-subtext {
            display: block;
            margin-top: 1.5rem;
            font-size: 0.9rem;
            color: var(--text-gray);
            text-transform: uppercase;
            letter-spacing: 0.2em;
        }

        /* 3D Rotating Phone */
        .phone-showcase {
            position: relative;
            perspective: 1000px;
        }

        .phone-3d {
            position: relative;
            width: 100%;
            max-width: 400px;
            margin: 0 auto;
            animation: rotate3d 20s ease-in-out infinite;
            transform-style: preserve-3d;
        }

        @keyframes rotate3d {
            0%, 100% { transform: rotateY(-15deg) rotateX(5deg); }
            50% { transform: rotateY(15deg) rotateX(-5deg); }
        }

        .phone-3d img {
            width: 100%;
            height: auto;
            filter: drop-shadow(0 30px 60px rgba(0, 217, 255, 0.5));
            border-radius: 40px;
        }

        .glow-ring {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 120%;
            height: 120%;
            border: 3px solid var(--electric-blue);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            opacity: 0.3;
            animation: ringPulse 3s ease-in-out infinite;
        }

        @keyframes ringPulse {
            0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.3; }
            50% { transform: translate(-50%, -50%) scale(1.1); opacity: 0.1; }
        }

        /* Section Styles */
        section {
            padding: 8rem 0;
            position: relative;
        }

        .section-header {
            text-align: center;
            max-width: 900px;
            margin: 0 auto 5rem;
        }

        .section-header h2 {
            font-family: 'Manrope', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 800;
            margin-bottom: 1.5rem;
            line-height: 1.2;
        }

        .section-header p {
            font-size: 1.2rem;
            color: var(--text-gray);
            line-height: 1.8;
        }

        /* Cards with Hover Effects */
        .card-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2rem;
        }

        .card {
            background: linear-gradient(135deg, rgba(157, 78, 221, 0.1), rgba(0, 217, 255, 0.05));
            border: 2px solid rgba(0, 217, 255, 0.2);
            border-radius: 30px;
            padding: 3rem;
            position: relative;
            overflow: hidden;
            transition: all 0.4s ease;
        }

        .card::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(0, 217, 255, 0.15), transparent 50%);
            opacity: 0;
            transition: opacity 0.4s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            border-color: var(--electric-blue);
            box-shadow: 0 20px 60px rgba(0, 217, 255, 0.3);
        }

        .card:hover::before {
            opacity: 1;
        }

        .card-icon {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            display: inline-block;
            animation: bounce 2s ease-in-out infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .card h3 {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--electric-blue);
        }

        .card p {
            color: var(--text-gray);
            line-height: 1.8;
            margin-bottom: 1.5rem;
        }

        .benefit-result {
            display: inline-block;
            padding: 0.5rem 1.5rem;
            background: linear-gradient(135deg, var(--neon-pink), var(--energy-yellow));
            color: var(--bg-dark);
            font-weight: 700;
            border-radius: 50px;
            font-size: 0.9rem;
        }

        /* Animated Counter */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 3rem;
            margin: 4rem 0;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 4rem;
            font-weight: 900;
            background: linear-gradient(135deg, var(--electric-blue), var(--neon-pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            display: block;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 1.1rem;
            color: var(--text-gray);
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        /* Pricing Cards */
        .pricing-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 3rem;
            margin: 4rem 0;
        }

        .pricing-card {
            background: linear-gradient(135deg, rgba(10, 14, 26, 0.8), rgba(5, 8, 16, 0.9));
            border: 2px solid rgba(0, 217, 255, 0.3);
            border-radius: 40px;
            padding: 3rem;
            position: relative;
            overflow: hidden;
            transition: all 0.4s ease;
        }

        .pricing-card.featured {
            border-color: var(--neon-pink);
            box-shadow: 0 0 60px rgba(255, 0, 128, 0.4);
            transform: scale(1.05);
        }

        .pricing-card.featured::before {
            content: 'ЛУЧШИЙ ВЫБОР';
            position: absolute;
            top: 2rem;
            right: -2rem;
            background: linear-gradient(135deg, var(--neon-pink), var(--power-purple));
            color: white;
            padding: 0.5rem 3rem;
            transform: rotate(45deg);
            font-size: 0.8rem;
            font-weight: 700;
            letter-spacing: 0.1em;
        }

        .pricing-card:hover {
            transform: translateY(-10px) scale(1.02);
        }

        .pricing-card h3 {
            font-size: 2rem;
            margin-bottom: 1rem;
        }

        .price {
            font-size: 3.5rem;
            font-weight: 900;
            margin: 2rem 0;
            background: linear-gradient(135deg, var(--electric-blue), var(--energy-yellow));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .price-note {
            font-size: 0.9rem;
            color: var(--text-gray);
            display: block;
            margin-top: 0.5rem;
        }

        .features-list {
            list-style: none;
            margin: 2rem 0;
        }

        .features-list li {
            padding: 1rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .features-list li::before {
            content: '✓';
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 24px;
            height: 24px;
            background: var(--electric-blue);
            color: var(--bg-dark);
            border-radius: 50%;
            font-weight: 900;
            flex-shrink: 0;
        }

        /* Form Styles */
        .lead-form {
            max-width: 600px;
            margin: 4rem auto 0;
            background: rgba(10, 14, 26, 0.8);
            border: 2px solid rgba(0, 217, 255, 0.3);
            border-radius: 30px;
            padding: 3rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-input {
            width: 100%;
            padding: 1.2rem 1.5rem;
            background: rgba(0, 217, 255, 0.05);
            border: 2px solid rgba(0, 217, 255, 0.2);
            border-radius: 15px;
            color: var(--text-white);
            font-size: 1rem;
            font-family: 'Golos Text', sans-serif;
            transition: all 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--electric-blue);
            box-shadow: 0 0 20px rgba(0, 217, 255, 0.3);
            background: rgba(0, 217, 255, 0.1);
        }

        .form-input::placeholder {
            color: var(--text-gray);
        }

        .submit-btn {
            width: 100%;
            padding: 1.5rem;
            background: linear-gradient(135deg, var(--electric-blue), var(--neon-pink));
            border: none;
            border-radius: 15px;
            color: white;
            font-size: 1.2rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 20px 40px rgba(0, 217, 255, 0.4);
        }

        /* Responsive */
        @media (max-width: 968px) {
            .hero-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .phone-showcase {
                order: -1;
                margin-bottom: 3rem;
            }

            .card-grid,
            .pricing-grid {
                grid-template-columns: 1fr;
            }

            .pricing-card.featured {
                transform: scale(1);
            }
        }

        /* Scroll Reveal Animation */
        .reveal {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s ease;
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>

    <div class="animated-bg"></div>

    <!-- Energy Particles -->
    <div class="energy-particle" style="left: 10%; animation-delay: 0s;"></div>
    <div class="energy-particle" style="left: 30%; animation-delay: 2s;"></div>
    <div class="energy-particle" style="left: 50%; animation-delay: 4s;"></div>
    <div class="energy-particle" style="left: 70%; animation-delay: 6s;"></div>
    <div class="energy-particle" style="left: 90%; animation-delay: 8s;"></div>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container hero-content">
            <div class="hero-text">
                <h1>Превращаем хаос на рецепции в системные продажи</h1>
                <h2><span class="highlight">FitBot</span> — операционная система для фитнес-клуба в Telegram, которая обнуляет расходы на SMS, забирает 100% рутины и работает как AI-помощник 24/7</h2>
                <a href="#final-cta" class="cta-btn">
                    <span style="position: relative; z-index: 1;">🚀 Рассчитать экономию</span>
                </a>
                <span class="cta-subtext">Окупается с первого месяца</span>
            </div>
            <div class="phone-showcase">
                <div class="glow-ring"></div>
                <div class="phone-3d">
                    <%= image_tag "bot_screenshot.png", alt: "FitBot Assistant Interface" %>
                </div>
            </div>
        </div>
    </section>

    <!-- Pain Points Section -->
    <section>
        <div class="container">
            <div class="section-header reveal">
                <h2>Узнаете себя в "Час Пик" (18:00–20:00)?</h2>
                <p>Если у вас сеть из 2-3 клубов, вы наверняка теряете деньги в этих трех «черных дырах»</p>
            </div>
            <div class="card-grid">
                <div class="card reveal">
                    <div class="card-icon">🔥</div>
                    <h3>«Администратор в огне»</h3>
                    <p>Вечер. На рецепции очередь. Админ одной рукой выдает ключи, другой отвечает на звонок «А бассейн работает?», третьей пытается пробить воду.</p>
                    <span class="benefit-result">❌ Продажи стоят. Сервис на нуле</span>
                </div>
                <div class="card reveal">
                    <div class="card-icon">💸</div>
                    <h3>«Деньги на ветер»</h3>
                    <p>Вы платите 15 000 ₽ в месяц за SMS-рассылки. Честно: кто их читает? А ваше мобильное приложение за 300к+ клиенты удаляют через неделю.</p>
                    <span class="benefit-result">❌ Вы платите за воздух</span>
                </div>
                <div class="card reveal">
                    <div class="card-icon">🚪</div>
                    <h3>«Тихий уход»</h3>
                    <p>Клиент не хочет скандалить на рецепции, он просто молча не продлевает абонемент и уходит к «Федералам», где сервис вылизан.</p>
                    <span class="benefit-result">❌ Теряете LTV</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Benefits Section -->
    <section style="background: linear-gradient(135deg, rgba(157, 78, 221, 0.05), transparent);">
        <div class="container">
            <div class="section-header reveal">
                <h2>FitBot меняет правила игры</h2>
                <p>Вы получаете сервис Федеральной сети по цене обеда</p>
            </div>

            <div class="stats-grid">
                <div class="stat-item reveal">
                    <span class="stat-number" data-count="15000">0</span>
                    <span class="stat-label">₽ Экономия / месяц</span>
                </div>
                <div class="stat-item reveal">
                    <span class="stat-number" data-count="90">0</span>
                    <span class="stat-label">% Рутины забирает бот</span>
                </div>
                <div class="stat-item reveal">
                    <span class="stat-number">24/7</span>
                    <span class="stat-label">AI-поддержка</span>
                </div>
            </div>

            <div class="card-grid">
                <div class="card reveal">
                    <div class="card-icon">💰</div>
                    <h3>Экономия с первого дня</h3>
                    <p>Заменяем SMS-рассылки полностью. Бот живет в Telegram — это канал, который клиент не может удалить, потому что там его пропуск и кошелек.</p>
                    <span class="benefit-result">+15 000 ₽ в кассе каждый месяц</span>
                </div>
                <div class="card reveal">
                    <div class="card-icon">⚡</div>
                    <h3>Админ теперь Продает</h3>
                    <p>Бот берет на себя вход, оплату, расписание и ответы на вопросы. Администратор освобождает 40% времени для живого общения.</p>
                    <span class="benefit-result">Рост продаж абонементов</span>
                </div>
                <div class="card reveal">
                    <div class="card-icon">🤖</div>
                    <h3>AI-Консьерж 24/7</h3>
                    <p>Бот обучен на регламентах вашего клуба. Он ответит на вопрос «Нужна ли шапочка?» в 3 часа ночи. Никаких «висений» на телефоне.</p>
                    <span class="benefit-result">Premium сервис</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Pricing Section -->
    <section>
        <div class="container">
            <div class="section-header reveal">
                <h2>Стоит дешевле ваших потерь на SMS</h2>
                <p>Честная модель. Результат с первого дня.</p>
            </div>

            <div class="pricing-grid">
                <div class="pricing-card reveal">
                    <h3>🚀 START</h3>
                    <p>Для тех, кто хочет навести порядок</p>
                    <div class="price">5 000 ₽<span class="price-note">/ за клуб в месяц</span></div>
                    <ul class="features-list">
                        <li>Цифровой пропуск</li>
                        <li>Онлайн-оплата</li>
                        <li>Умный автоответчик</li>
                        <li>Актуальное расписание</li>
                    </ul>
                </div>

                <div class="pricing-card featured reveal">
                    <h3>🔥 PRO</h3>
                    <p>Для тех, кто хочет расти</p>
                    <div class="price">START + 10K<span class="price-note">/ за всю сеть</span></div>
                    <ul class="features-list">
                        <li>Всё из Start</li>
                        <li>Прямые рассылки в Telegram</li>
                        <li>Нативная реклама акций</li>
                        <li>Расширенная аналитика</li>
                        <li>AI-сценарии возврата клиентов</li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <!-- Final CTA -->
    <section id="final-cta" style="background: radial-gradient(circle at top, rgba(0, 217, 255, 0.1), transparent 50%);">
        <div class="container">
            <div class="section-header reveal">
                <h2>Готовы превратить качалку в цифровую экосистему?</h2>
                <p>Не ждите, пока к вам в город придут Федералы. Опередите их по сервису уже сегодня.</p>
            </div>

            <form class="lead-form reveal">
                <div class="form-group">
                    <input type="text" class="form-input" placeholder="Ваше Имя" required>
                </div>
                <div class="form-group">
                    <input type="tel" class="form-input" placeholder="Ваш Телефон" required>
                </div>
                <div class="form-group">
                    <input type="number" class="form-input" placeholder="Количество клубов в сети" required>
                </div>
                <button type="submit" class="submit-btn">Получить демо</button>
            </form>
            <p style="text-align: center; margin-top: 2rem; color: var(--text-gray);">Интеграция занимает 3 дня. Работаем по всей РФ и СНГ</p>
        </div>
    </section>

    <script>
        // Scroll Reveal Animation
        const reveals = document.querySelectorAll('.reveal');

        const revealOnScroll = () => {
            reveals.forEach(element => {
                const elementTop = element.getBoundingClientRect().top;
                const elementVisible = 150;

                if (elementTop < window.innerHeight - elementVisible) {
                    element.classList.add('active');
                }
            });
        };

        window.addEventListener('scroll', revealOnScroll);
        revealOnScroll();

        // Animated Counter
        const counters = document.querySelectorAll('.stat-number[data-count]');

        const animateCounter = (counter) => {
            const target = parseInt(counter.getAttribute('data-count'));
            const duration = 2000;
            const step = target / (duration / 16);
            let current = 0;

            const updateCounter = () => {
                current += step;
                if (current < target) {
                    counter.textContent = Math.floor(current).toLocaleString('ru-RU');
                    requestAnimationFrame(updateCounter);
                } else {
                    counter.textContent = target.toLocaleString('ru-RU');
                }
            };

            updateCounter();
        };

        const observerOptions = {
            threshold: 0.5,
            rootMargin: '0px'
        };

        const counterObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting && !entry.target.classList.contains('counted')) {
                    animateCounter(entry.target);
                    entry.target.classList.add('counted');
                }
            });
        }, observerOptions);

        counters.forEach(counter => counterObserver.observe(counter));

        // Form Submission
        document.querySelector('.lead-form').addEventListener('submit', (e) => {
            e.preventDefault();
            alert('Спасибо! Мы свяжемся с вами в течение 1 часа! 🚀');
        });
    </script>

</body>
</html>
