<style>
  @import url('https://fonts.googleapis.com/css2?family=Clash+Display:wght@500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap');

  :root {
    --bg: #03010b;
    --bg-alt: #0b0417;
    --ink: #f6f3ff;
    --muted: rgba(246, 243, 255, 0.7);
    --accent: #fdbb2d;
    --accent-2: #7efaff;
    --card: rgba(9, 6, 20, 0.85);
    --card-light: rgba(255, 255, 255, 0.08);
    --border: rgba(255, 255, 255, 0.2);
  }

  .landing-looser,
  .landing-looser * {
    box-sizing: border-box;
    font-family: 'Space Grotesk', 'Clash Display', sans-serif;
  }

  .landing-looser {
    position: relative;
    overflow: hidden;
    background: radial-gradient(circle at top, rgba(255, 186, 61, 0.18), transparent 40%),
      radial-gradient(circle at 20% 20%, rgba(126, 250, 255, 0.15), transparent 45%),
      linear-gradient(135deg, #020007 0%, #080313 45%, #05010f 100%);
    color: var(--ink);
    padding: 0;
  }

  .landing-looser::before,
  .landing-looser::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .landing-looser::before {
    background-image: linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
    background-size: 80px 80px;
    opacity: 0.4;
  }

  .landing-looser::after {
    background-image: url('https://www.transparenttextures.com/patterns/dark-matter.png');
    mix-blend-mode: screen;
    opacity: 0.15;
  }

  .lv-shell {
    width: min(1180px, calc(100% - 3rem));
    margin: 0 auto;
    position: relative;
    z-index: 2;
  }

  .lv-hero {
    position: relative;
    padding: clamp(4rem, 12vw, 8rem) 0 4rem;
  }

  .lv-hero__halo {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(255, 198, 92, 0.3), transparent 55%);
    filter: blur(50px);
    z-index: 0;
  }

  .lv-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.35rem 1.2rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.25);
    background: rgba(255, 255, 255, 0.06);
    letter-spacing: 0.22em;
    text-transform: uppercase;
    font-size: 0.75rem;
    color: var(--accent-2);
    margin-bottom: 1.5rem;
  }

  .lv-title {
    font-family: 'Clash Display', 'Space Grotesk', sans-serif;
    font-size: clamp(3.2rem, 8vw, 6rem);
    line-height: 1.05;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .lv-title span {
    color: var(--accent);
  }

  .lv-subtitle {
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    color: var(--muted);
    max-width: 680px;
    margin-bottom: 2.5rem;
  }

  .lv-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 3rem;
  }

  .lv-button {
    padding: 1rem 2.6rem;
    border-radius: 999px;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    border: 1px solid transparent;
    transition: 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
  }

  .lv-button.primary {
    background: linear-gradient(120deg, #ffb347, #ffcc33, #ffd966);
    color: #1b1200;
    box-shadow: 0 20px 40px rgba(253, 187, 45, 0.4);
  }

  .lv-button.secondary {
    border-color: rgba(255, 255, 255, 0.4);
    color: var(--ink);
    background: transparent;
  }

  .lv-button:hover {
    transform: translateY(-3px);
  }

  .lv-stat-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.25rem;
  }

  .lv-stat-card {
    padding: 1.75rem;
    border-radius: 1.5rem;
    background: var(--card);
    border: 1px solid var(--border);
    box-shadow: inset 0 0 20px rgba(255, 255, 255, 0.03), 0 25px 50px rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(12px);
    animation: float 6s ease-in-out infinite;
  }

  .lv-stat-card:nth-child(2) {
    animation-delay: 1.5s;
  }

  .lv-stat-value {
    font-size: clamp(2.8rem, 5vw, 3.8rem);
    font-weight: 700;
    letter-spacing: -0.03em;
  }

  .lv-stat-label {
    font-size: 0.88rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--muted);
  }

  .lv-section {
    padding: clamp(4rem, 10vw, 7rem) 0;
    position: relative;
  }

  .lv-section--light {
    background: #f7f5ff;
    color: #0f0720;
  }

  .lv-section__header {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .lv-tag {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.4rem 1.3rem;
    border-radius: 999px;
    border: 1px solid currentColor;
    font-size: 0.75rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }

  .lv-section h2 {
    font-family: 'Clash Display', sans-serif;
    font-size: clamp(2.4rem, 5vw, 3.5rem);
    margin-bottom: 1rem;
  }

  .lv-section p.lead {
    color: var(--muted);
    max-width: 640px;
    margin: 0 auto;
  }

  .lv-section--light p.lead {
    color: rgba(15, 7, 32, 0.7);
  }

  .lv-problem-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.8rem;
  }

  .lv-problem-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid var(--border);
    border-radius: 1.4rem;
    padding: 1.8rem;
    min-height: 280px;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    position: relative;
    overflow: hidden;
  }

  .lv-problem-card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(145deg, rgba(255, 179, 71, 0.05), rgba(126, 250, 255, 0.02));
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .lv-problem-card:hover::after {
    opacity: 1;
  }

  .lv-problem-card strong {
    font-size: 1.5rem;
    color: var(--accent);
  }

  .lv-loss-callout {
    margin-top: 3.2rem;
    border-radius: 2rem;
    padding: 3rem;
    background: linear-gradient(120deg, rgba(253, 110, 80, 0.95), rgba(253, 187, 45, 0.85));
    text-align: center;
    color: #1c0500;
    box-shadow: 0 25px 55px rgba(253, 164, 45, 0.35);
  }

  .lv-loss-callout h3 {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    margin: 0.7rem 0;
  }

  .lv-solution-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
  }

  .lv-solution-card {
    background: white;
    border-radius: 1.5rem;
    padding: 2rem;
    box-shadow: 0 25px 65px rgba(10, 9, 30, 0.12);
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .lv-solution-card h3 {
    font-size: 1.2rem;
    font-weight: 600;
  }

  .lv-solution-card ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    color: rgba(15, 7, 32, 0.75);
  }

  .lv-pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .lv-price-card {
    background: var(--card);
    border-radius: 2rem;
    padding: 3rem 2.5rem;
    border: 1px solid var(--border);
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    min-height: 520px;
  }

  .lv-price-card--accent {
    background: linear-gradient(140deg, rgba(255, 255, 255, 0.15), rgba(255, 204, 48, 0.12));
    border-color: rgba(255, 204, 72, 0.6);
    box-shadow: 0 30px 80px rgba(255, 204, 72, 0.25);
  }

  .lv-badge {
    position: absolute;
    top: 1.5rem;
    left: 50%;
    transform: translateX(-50%);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1.6rem;
    border-radius: 999px;
    border: 1px solid currentColor;
    font-size: 0.8rem;
    backdrop-filter: blur(6px);
  }

  .lv-price-card h3 {
    margin-top: 3.5rem;
    font-size: 2.2rem;
    letter-spacing: 0.08em;
  }

  .lv-price-card .price {
    font-size: clamp(2.6rem, 5vw, 3.2rem);
    font-weight: 700;
  }

  .lv-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
  }

  .lv-list li {
    display: flex;
    gap: 0.6rem;
    align-items: center;
  }

  .lv-list svg {
    width: 18px;
    height: 18px;
    color: var(--accent-2);
  }

  .lv-cta {
    margin-top: 4rem;
    border-radius: 2rem;
    background: linear-gradient(120deg, #6b46ff, #ff5c8d, #ffba2d);
    padding: clamp(2.5rem, 6vw, 4rem);
    text-align: center;
    color: #fffaf2;
    box-shadow: 0 25px 70px rgba(255, 92, 141, 0.35);
  }

  .lv-contact {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    margin-top: 2rem;
  }

  .lv-contact a {
    padding: 0.9rem 1.8rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.4);
    color: inherit;
    text-decoration: none;
    font-weight: 600;
  }

  .lv-footer {
    padding: 2.5rem 0 4rem;
    text-align: center;
    color: rgba(255, 255, 255, 0.55);
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @media (max-width: 640px) {
    .lv-button {
      width: 100%;
      justify-content: center;
    }

    .lv-price-card {
      padding: 2.5rem 1.75rem;
    }
  }
</style>

<div class="landing-looser">
  <div class="lv-hero">
    <div class="lv-hero__halo"></div>
    <div class="lv-shell">
      <p class="lv-eyebrow">FITBOT • DIGITAL OPS</p>
      <h1 class="lv-title">
        Превращаем фитнес-клуб <span>в прибыльную систему</span>
      </h1>
      <p class="lv-subtitle">
        Автоматизируем вход, оплату, коммуникации и сервис через Telegram. Администратор больше не выдаёт ключи — он продаёт, а сеть растёт.
      </p>
      <div class="lv-buttons">
        <a class="lv-button primary" href="#pricing">Увидеть ROI</a>
        <a class="lv-button secondary" href="#solution">Смотреть стек</a>
      </div>
      <div class="lv-stat-row">
        <article class="lv-stat-card">
          <div class="lv-stat-value">95К ₽</div>
          <p class="lv-stat-label">Потери без нас / месяц</p>
        </article>
        <article class="lv-stat-card">
          <div class="lv-stat-value">100%</div>
          <p class="lv-stat-label">Автоматизация рутины</p>
        </article>
        <article class="lv-stat-card">
          <div class="lv-stat-value">7 дней</div>
          <p class="lv-stat-label">До запуска бота</p>
        </article>
      </div>
    </div>
  </div>

  <section class="lv-section" id="problems">
    <div class="lv-shell">
      <div class="lv-section__header">
        <p class="lv-tag">Проблемы</p>
        <h2>Региональные сети теряют деньги в тишине</h2>
        <p class="lead">Бумагой клуб прибыльный, но реальность — хаос админа, дорогие SMS и клиенты, которые молча уходят.</p>
      </div>
      <div class="lv-problem-grid">
        <article class="lv-problem-card">
          <strong>Админ = выдаёт ключи</strong>
          <p>В пике 18:00–20:00 стойка работает как кол-центр. Продажи стоят, заявки не успевают.</p>
          <p>−30 000 ₽ ежемесячно</p>
        </article>
        <article class="lv-problem-card">
          <strong>Чёрная дыра коммуникаций</strong>
          <p>SMS на 15 тыс ₽ читают единицы. Приложение удаляется на второй день. Клиент не слышит клуб.</p>
          <p>−15 000 ₽ прямого бюджета</p>
        </article>
        <article class="lv-problem-card">
          <strong>Тихий отток</strong>
          <p>Поломка тренажёра или грязная душевые — клиент не жалуется, а просто уходит к федералам.</p>
          <p>−50 000 ₽ из-за сервиса</p>
        </article>
      </div>
      <div class="lv-loss-callout">
        <p>Суммарный прогар без FitBot</p>
        <h3>95 000 ₽ в месяц</h3>
        <p>Вы платите не нам — вы платите за хаос.</p>
      </div>
    </div>
  </section>

  <section class="lv-section lv-section--light" id="solution">
    <div class="lv-shell">
      <div class="lv-section__header">
        <p class="lv-tag">Решение</p>
        <h2>FitBot — операционная система клуба</h2>
        <p class="lead">Мы забираем 100% рутины в Telegram, где клиент точно живёт. Администратор получает данные и продаёт.</p>
      </div>
      <div class="lv-solution-grid">
        <article class="lv-solution-card">
          <h3>Автоматизация 24/7</h3>
          <p>Вход, карта, оплата, записи и напоминания в одном диалоге.</p>
          <ul>
            <li>• Цифровой пропуск в Telegram</li>
            <li>• Оплата в 2 клика + повтор по ссылке</li>
            <li>• Автозапись и контроль доступов</li>
          </ul>
        </article>
        <article class="lv-solution-card">
          <h3>Экономия на связи</h3>
          <p>SMS и приложения уходят в прошлое: Telegram-бот доставляет 100% сообщений.</p>
          <ul>
            <li>• 0 ₽ на рассылки</li>
            <li>• Сегментированные промо внутри бота</li>
            <li>• Глубокая аналитика кликов</li>
          </ul>
        </article>
        <article class="lv-solution-card">
          <h3>Обратная связь, которая работает</h3>
          <p>Жалобы и благодарности моментально улетают нужному сотруднику.</p>
          <ul>
            <li>• Маршрутизация по роли</li>
            <li>• Контроль SLA внутри Telegram</li>
            <li>• Предотвращаем отток до того, как он случился</li>
          </ul>
        </article>
      </div>
    </div>
  </section>

  <section class="lv-section" id="pricing">
    <div class="lv-shell">
      <div class="lv-section__header">
        <p class="lv-tag">Цены</p>
        <h2>Мы продаём экономию и рост</h2>
        <p class="lead">Платите меньше, чем теряете сейчас. Настраиваем, интегрируем, сопровождаем.</p>
      </div>
      <div class="lv-pricing-grid">
        <article class="lv-price-card">
          <div class="lv-badge" style="color: var(--accent-2);">🚀 Популярный</div>
          <h3>FitBot START</h3>
          <p class="price">от 5 000 ₽ / клуб</p>
          <p>Закрываем основные дыры и переводим клиентов в цифровой поток.</p>
          <ul class="lv-list">
            <li>✔ Цифровая карта и пропуск</li>
            <li>✔ Онлайн-оплата + автосписания</li>
            <li>✔ Актуальное расписание и запись</li>
            <li>✔ Канал обратной связи</li>
          </ul>
          <p style="margin-top: auto; color: var(--accent); font-weight: 600;">Экономия ~15 000 ₽/мес</p>
        </article>
        <article class="lv-price-card lv-price-card--accent">
          <div class="lv-badge" style="color: #1b1200; background: rgba(255, 255, 255, 0.7);">🔥 Для роста</div>
          <h3>FitBot PRO</h3>
          <p class="price">+10 000 ₽ / сеть</p>
          <p>Для сетей, которые качают выручку через персональные коммуникации и аналитику.</p>
          <ul class="lv-list">
            <li>✔ Все из START</li>
            <li>✔ CRM-инструменты для администратора и РОПа</li>
            <li>✔ Сегментированные рассылки в боте</li>
            <li>✔ Нативные продажи акций</li>
            <li>✔ Переписка с клиентами + аналитика</li>
          </ul>
          <p style="margin-top: auto; color: #1b1200; font-weight: 700;">Экономия + дополнительный доход</p>
        </article>
      </div>
      <div class="lv-cta" id="contact">
        <h3>Интеграция за 7 дней · 15 000 ₽ разово / клуб</h3>
        <p>Связываем с 1С:Фитнес или вашей CRM, настраиваем бота, обучаем персонал.</p>
        <div class="lv-contact">
          <a href="mailto:info@fitbot.ru">info@fitbot.ru</a>
          <a href="tel:+79991234567">+7 (999) 123-45-67</a>
          <a href="#hero">Запланировать созвон</a>
        </div>
      </div>
    </div>
  </section>

  <footer class="lv-footer">
    <div class="lv-shell">
      <p>© 2025 FitBot — превращаем расход в доход. Интегрируем, автоматизируем, сопровождаем.</p>
    </div>
  </footer>
</div>
